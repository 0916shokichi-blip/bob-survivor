# Changelog

## 2026-04-29 — スキル拡張バッチ 1（7 種追加）

`bob_survival_skills_30.md` から ◎/○ 判定で実装容易な 7 つを追加。攻撃 1 + 防御/補助 6 のバランス。

### 武器

- **拈華（ねんげ）** — 向いた方向に扇形の停止フィールドを発する。ダメージなし、敵を 1.25–2.65s 停止させる。CD 4.15s→2.75s。Lv で射程と停止時間が伸びる

### パッシブ

- **不動（ふどう）** — 静止時、被弾を確率で無効。Lv1=25% / Lv2=50% / Lv3=75%
- **緩歩（かんぽ）** — 移動 -10%/Lv & 攻撃間隔 -10%/Lv（max 3）。リスクリターン型
- **余白（よはく）** — 周囲 100px 内に煩悩 0 のとき全ダメ +30%/Lv（max 3）
- **静坐（せいざ）** — 被ダメ間隔（iframe）+0.15s/Lv（max 3）
- **結跏（けっか）** — 静止 0.6s 以上で内省の角速度 +25%/Lv（max 3）
- **一期一会（いちごいちえ）** — 一度倒した三毒（瞋・貪・痴）の種類には以後 ×2 ダメ。Lv1 固定

### 既存スキルとの相互作用メモ

- 緩歩は沈黙と乗算で複利（沈黙 Lv5 + 緩歩 Lv3 で攻撃間隔 0.6 * 0.7 = 0.42 倍）。歩み + 緩歩は反対向きで、両方取ると相殺気味
- 結跏は内省を持っている前提のスキル。前提依存だが選択は許容
- 不動は静止が条件、歩みは移動速度を上げる。立ち回り思想がぶつかる
- 余白は接近戦の緊張を緩める「離脱戦術」を成立させる
- 一期一会は三毒（瞋・貪・痴）の type-tag を流用。3 種すべて 1 度ずつ倒せば実質全敵 ×2

### 実装メモ

- 余白・一期一会の倍率は `damageEnemy()` に集約（呼び出し側は dmg を計算するだけ）
- `state.stillTime` で静止秒数を tracking、不動・結跏が共有
- `state.encounteredTypes`（Set）で遭遇 type を記録、`processDeaths()` で更新
- 拈華は `p.nenge` フラグ付き projectile。`age` ベース、`life=0.55s`、出現時に 1 度だけ stun を付与
- enemy の `stunUntil` は移動更新でチェック、足元に淡い円で表示
